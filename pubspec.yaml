name: word_search_master
description: "Word Search Master — relaxed, offline-first Urdu/Hindi/English word puzzle"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.1+4

environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.2
  riverpod_annotation: ^4.0.6
  go_router: ^18.0.0
  firebase_core: ^4.14.0
  characters: ^1.4.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.3
  unorm_dart: ^0.3.2
  freezed_annotation: ^3.1.0
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  crypto: ^3.0.7
  shared_preferences: ^2.5.5
  audioplayers: ^6.8.1
  firebase_auth: ^6.6.1
  firebase_app_check: ^0.4.7
  cloud_firestore: ^6.9.0
  cloud_functions: ^6.4.0
  firebase_analytics: ^12.5.0
  firebase_crashlytics: ^5.3.0
  firebase_remote_config: ^6.6.0
  google_sign_in: ^7.2.0
  connectivity_plus: ^7.3.1
  share_plus: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.16.0
  riverpod_generator: ^4.0.8
  freezed: ^4.0.0
  drift_dev: ^2.34.5
  sqlite3: ^3.5.2
  riverpod: ^3.4.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Required for `flutter gen-l10n` to produce importable localizations.
  generate: true

  assets:
    # Accidental-word blocklists, scanned after filling (Ch06). Kept as assets
    # so a native speaker can correct them without touching Dart. P10 adds the
    # word and level packs alongside these.
    - assets/content/
    # Ch03 SFX plus the looping background bed — synthesised tones, see
    # tool/generate_audio_assets.py. 61KB of SFX and a 250KB 8-second music
    # loop, 332KB in all, inside the 400KB budget. The loop is 16kHz mono
    # (the SFX are 22.05kHz) precisely to buy that headroom, which a low
    # mellow pad loses nothing audible to.
    - assets/audio/

  # Fonts are BUNDLED, never taken from the device: many budget Android
  # phones in PK/IN ship without a Urdu-capable font, and the result is a
  # grid of tofu boxes (Ch04). Family names here must match AppFonts in
  # lib/app/theme/app_typography.dart.
  #
  # These are the full Noto faces (~2.7 MB). P22 subsets them with fonttools
  # to only the glyph ranges the content actually uses, which brings this
  # down by roughly an order of magnitude before release.
  fonts:
    # Urdu GRID cells — horizontal and balanced, sits square in a cell.
    - family: NotoNaskhArabic
      fonts:
        - asset: assets/fonts/NotoNaskhArabic-Regular.ttf
        - asset: assets/fonts/NotoNaskhArabic-Bold.ttf
          weight: 700
    # Urdu DISPLAY/HEADING/word-chip only — never a grid cell.
    - family: NotoNastaliqUrdu
      fonts:
        - asset: assets/fonts/NotoNastaliqUrdu-Regular.ttf
        - asset: assets/fonts/NotoNastaliqUrdu-Bold.ttf
          weight: 700
    - family: NotoSansDevanagari
      fonts:
        - asset: assets/fonts/NotoSansDevanagari-Regular.ttf
        - asset: assets/fonts/NotoSansDevanagari-Bold.ttf
          weight: 700
    - family: NotoSans
      fonts:
        - asset: assets/fonts/NotoSans-Regular.ttf
        - asset: assets/fonts/NotoSans-Bold.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
